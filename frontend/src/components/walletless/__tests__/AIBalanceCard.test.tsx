// src/components/walletless/__tests__/AIBalanceCard.test.tsx
import { describe, it, expect } from 'vitest';
import AIBalanceCard from '../AIBalanceCard';

describe('AIBalanceCard', () => {
  it('is properly exported', () => {
    expect(AIBalanceCard).toBeDefined();
  });
});