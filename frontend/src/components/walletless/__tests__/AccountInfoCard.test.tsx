// src/components/walletless/__tests__/AccountInfoCard.test.tsx
import { describe, it, expect } from 'vitest';
import AccountInfoCard from '../AccountInfoCard';

describe('AccountInfoCard', () => {
  it('is properly exported', () => {
    expect(AccountInfoCard).toBeDefined();
  });
});